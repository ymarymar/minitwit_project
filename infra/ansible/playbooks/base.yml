- name: Base setup for all nodes
  hosts: swarm,db,monitoring
  become: true

  tasks:
    - name: Wait for apt lock to be released
      shell: while fuser /var/lib/apt/lists/lock /var/lib/dpkg/lock-frontend >/dev/null 2>&1; do sleep 5; done
      changed_when: false

    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install Docker
      shell: curl -fsSL https://get.docker.com | sh
      args:
        creates: /usr/bin/docker
      retries: 3
      delay: 30
      register: result
      until: result.rc == 0

    - name: Start and enable Docker
      systemd:
        name: docker
        state: started
        enabled: yes

    - name: Install Docker Compose plugin
      apt:
        name: docker-compose-plugin
        state: present

    - name: Install node exporter
      unarchive:
        src: https://github.com/prometheus/node_exporter/releases/download/v1.8.1/node_exporter-1.8.1.linux-amd64.tar.gz
        dest: /usr/local/bin
        remote_src: yes
        extra_opts: [--strip-components=1]
        creates: /usr/local/bin/node_exporter

    - name: Create node exporter systemd service
      copy:
        dest: /etc/systemd/system/node_exporter.service
        content: |
          [Unit]
          Description=Node Exporter
          After=network.target

          [Service]
          ExecStart=/usr/local/bin/node_exporter
          Restart=on-failure

          [Install]
          WantedBy=multi-user.target

    - name: Start and enable node exporter
      systemd:
        name: node_exporter
        state: started
        enabled: yes
        daemon_reload: yes
    
    - name: Create alloy config directory
      file:
        path: /etc/alloy
        state: directory

- name: Setup alloy on non-monitoring nodes
  hosts: swarm,db
  become: true
  vars_files:
    - vars/main.yml
  tasks:
    - name: Copy alloy config
      template:
        src: templates/alloy-config.alloy.j2
        dest: /etc/alloy/config.alloy

    - name: Restart alloy container
      docker_container:
        name: alloy
        image: grafana/alloy:latest
        state: started
        restart: yes
        restart_policy: always
        volumes:
          - /etc/alloy/config.alloy:/etc/alloy/config.alloy
          - /var/lib/docker/containers:/var/lib/docker/containers:ro
          - /var/run/docker.sock:/var/run/docker.sock

- name: Copy alloy config to monitoring node
  hosts: monitoring
  become: true
  tasks:
    - name: Copy alloy config
      copy:
        src: files/alloy-config-monitoring.alloy
        dest: /etc/alloy/config.alloy